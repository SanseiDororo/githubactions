name: Clone Existing Repository With Checkout
on: workflow_dispatch
jobs:
  create_environment:
    runs-on: ubuntu-latest
    steps:
      - name: Install Pipenv
        run: pip install pipenv
      - name: Copy the code
        uses: actions/checkout@v3
     
         

